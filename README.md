# google-cloud-run example project

## build image locally

`make build-container`

## build and push container to google container registry

`make build-and-push-container`

## get deployed service url

`make gcloud-get-deployed-url`
